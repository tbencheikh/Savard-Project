# Ansible Configuration

This directory contains Ansible playbooks and roles to configure the Windows server for Savard International Illimited.

## Usage
1. Run a playbook:
   ```bash
   ansible-playbook playbooks/ad-config.yml